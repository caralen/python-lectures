# UGRAĐENE FUNKCIJE


# neke smo upoznali i zvali smo ih naredbama, a u stvari su funkcije, a neke cemo upoznati

    # print()
    # input()
    # type()
    # konverzije int(), float(), bool(), chr(), bin(), hex(), str()
    # string upper(), lower(), split(), join(), repace(), len(), strip(), replace(), count(), capitalize()

    # liste reverse(), reversed(), 
        # random.randint(), random.shaffle(), random.randrange(), random.choice()

    # rjecnik clear(), items(), keys(), values(), pop(item[key]) popitem()

help(print)

